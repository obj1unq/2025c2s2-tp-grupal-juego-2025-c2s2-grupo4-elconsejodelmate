import wollok.game.*
import martina.*
program catastrofico{
    game.height(15)
    game.width(15)
    game.cellSize(30)
    game.title("Cat-astrofico")
    game.addVisual(martina)
    config.configTeclas()
    game.start()
}